# Main dependencies only (minimal)

# 1) System dependencies (WSL/Ubuntu)
# Install with:
# sudo apt update
# sudo apt install -y clang llvm clang-tidy cppcheck make python3 python3-venv python3-pip

clang
llvm
clang-tidy
cppcheck
make
python3
python3-venv
python3-pip

# 2) Python dependencies (inside .venv)
# Install with:
# pip install requests pandas tabulate matplotlib streamlit python-pptx

requests
pandas
tabulate
matplotlib
streamlit
python-pptx
