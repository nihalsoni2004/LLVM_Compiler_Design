# Source code

This folder is intended for project source code (analysis tools, helpers, or libraries).

Currently the project uses `scripts/` for automation and `ui/` for the Streamlit UI.
Place any core Python modules or C helper code under `src/`.
