# Testcases / Benchmarks

This directory is intended to contain the benchmark C programs used for UB experiments.

If `benchmarks/` already exists, use `scripts/sync_testcases.sh` to copy them into `testcases/`:

```bash
bash scripts/sync_testcases.sh
```
